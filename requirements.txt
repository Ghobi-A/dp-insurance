-e .[app]

# Known-good pinned versions (verified by executing the full notebook and test
# suite). For flexible ranges, install via `pip install -e .` instead.
numpy==1.24.3
pandas==2.3.3
scipy==1.10.1
scikit-learn==1.3.2
matplotlib==3.7.5

# DP-SGD (optional; CPU wheels: pip install torch --index-url https://download.pytorch.org/whl/cpu)
torch==2.12.1
opacus==1.6.0
